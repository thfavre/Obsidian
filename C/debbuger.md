#C

```Shell
gcc -Werror -Wall -Wextra -g3 file.c lldb ./a.out b (breakpoint de la ou tu veux commencer, la ligne, b 15 ou b main, par exemple) r (r pour run) gui (pour ouvrir l'interface) n (pour avancer)
```